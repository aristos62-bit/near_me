import 'package:flutter/material.dart';

class AnonymousHomeScreen extends StatelessWidget {
  const AnonymousHomeScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('NearMe')));
}
